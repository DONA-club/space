import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.donaclubspace',
  appName: 'DONA.club Space',
  webDir: 'dist'
};

export default config;
